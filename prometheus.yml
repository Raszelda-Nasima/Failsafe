global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "failsafe-api"
    static_configs:
      # Fixed HTTP port 5171, matching the explicit UseUrls binding above.
      - targets: ["host.docker.internal:5171"]